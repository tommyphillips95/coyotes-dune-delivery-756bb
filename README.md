# H.A.C.D.

Hyperscale Auto Consultation Designer. Static site for Netlify.

Publish directory is the repo root (`netlify.toml`). Open `index.html`. The page scores a campus brief and draws a schematic set. It is a planning tool, not sealed engineering.

The old placeholder said the Netlify site was live. It was not. Connect this repo in Netlify, then the generated site URL is the one to send.
